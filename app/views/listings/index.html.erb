<h1>All listings</h1>
<% Listing.all.each do |listing| %>
	<p><%= "#{listing.user.first_name} #{listing.user.last_name}" %></p>
	<p><%= "#{listing.title}" %></p>
	<p><%= "#{listing.city.description}, #{listing.state.description}" %></p>
	<p><%= "#{listing.price}" %></p>
<% end %>